# Rendered by dev-servers/scripts/install.sh into
# _data/conan-exiles/bridge/TakaroConfig.txt

# Takaro connection
registrationToken=${TAKARO_REGISTRATION_TOKEN}
identityToken=${IDENTITY_TOKEN}
serverName=${SERVER_NAME}
takaroWsUrl=${TAKARO_WS_URL}

# Conan Exiles RCON — reached over the compose network, not the host.
rconHost=conan-exiles
rconPort=25575
rconPassword=${RCON_PASSWORD}
rconCommandGapMs=1000

# Bridge runtime
httpPort=3010
pollIntervalMs=10000
enableLogEvents=true
requireModSourceAttribution=false

# Optional read-only Conan save database and item catalog. Left blank: the
# save DB only exists after the server has generated a world, and the item
# catalog needs a DevKit ItemTable export. See games/conan-exiles/README.md.
databasePath=
itemCatalogPath=

# Server log, bind-mounted read-only into the bridge container.
logFiles=/bridge/logs/ConanSandbox.log
