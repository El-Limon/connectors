# Read by .github/workflows/maintenance.yml on every scheduled run. Tracked so that enabling
# scheduled publication is a reviewed change on main, not a repository setting.
# Values: disabled | enabled. The rollout ticket flips it after the rollout is proven.
publish_schedule: disabled
cron: "17 */6 * * *"
